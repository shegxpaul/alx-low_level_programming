#include "main.h"

/**
 * _abs - absolute value of an integer
 * @t: th einteger to be computed
 * Return: the sbsolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

